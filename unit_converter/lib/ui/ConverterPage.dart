import 'package:flutter/material.dart';

class ConverterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        backgroundColor: Colors.blue[400],
        title: new Text("Unit's Name"),
      ),
      body: new Container(
        color: Colors.blue[100],
      ),
    );
  }
}
